services:
  rag-chatbot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/app:cached
    # Override the CMD to ensure the file exists
    command: ["sh", "-c", "ls -la /app && chainlit run main2.py --port 8000 --host 0.0.0.0"]
